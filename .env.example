# Database
DATABASE_URL="postgresql://username:password@localhost:5432/finished_project_management"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here" # Generate with: openssl rand -base64 32

# Email (Optional - for email verification)
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""
EMAIL_SERVER_HOST=""
EMAIL_SERVER_PORT=""
EMAIL_FROM=""

# Required Variables:
# - DATABASE_URL: PostgreSQL connection string
# - NEXTAUTH_SECRET: Random secret for JWT signing (generate with openssl rand -base64 32)
# 
# Optional Variables:
# - EMAIL_*: For email verification (can be left empty for development)
# - NEXTAUTH_URL: Automatically set by Vercel in production

# Development Setup:
# 1. Copy this file to .env.local
# 2. Update DATABASE_URL with your PostgreSQL connection
# 3. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 4. Run: npx prisma migrate dev
# 5. Run: npx prisma db seed (after creating seed file)